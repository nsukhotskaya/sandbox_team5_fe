import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#de9474',
      light: '#40bfef',
    },
    secondary: {
      main: '#e4ebef',
      dark: '#929292',
      light: '#E9EFF2',
    },
  },
});

export default theme;
