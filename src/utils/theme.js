import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0082ca',
      light: '#40bfef',
    },
    secondary: {
      main: '#e4ebef',
      dark: '#929292',
    },
  },
});

export default theme;
