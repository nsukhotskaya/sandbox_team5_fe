import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0082ca',
      light: '#f2f2f2',
    },
    background: {
      default: '#d6ebff',
    },
    text: {
      secondary: '#929292',
    },
  },
});

export default theme;
